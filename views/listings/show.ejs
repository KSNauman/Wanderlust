<% layout('layouts/boilerplate') %>

    <div class="row">
        <div class="col-8 offset-3">
            <p class="h2">
                <%= post.title %>
            </p>
            
        </div>
        
        <div class="card col-6 offset-3 show-card">
            <img src="<%=post.img%>" class="card-img-top" alt="Listing image">
            <div class="card-body">
                <p class="card-text">Owned By <i><%= post.owner.username %></i></p>
                <p class="card-text"><%=post.description %></p>
                <p class="card-text">&#8377;<%=post.price.toLocaleString("en-IN") %></p>
                <p class="card-text"><%=post.location %></p>
                <p class="card-text"><%=post.country %></p>
            </div>
        </div>
        <!-- Button Row -->
        <% if (currUser && currUser._id.equals(post.owner._id)) { %>
            <div class="col-6 offset-3 d-flex justify-content-between mt-3">
                <a class="btn btn-dark" href="/listing/<%= post.id %>/edit">Edit</a>
                <form action="/listing/<%= post.id%>?_method=DELETE" method="POST">
                    <button class="btn add-btn">Delete</button>
                </form>
            </div>
        <% } %>
        <div class="col-8 offset-3 mb-3 mt-3">
            <% if (condition) { %>
             
            <% } %>
            <!-- Review part  -->
            <hr>
            <h4>Leave a Review</h4>
            <form action="/listing/<%= post.id %>/reviews" method="post" novalidate class="needs-validation">
                <div class="rating">
                    <label for="rating" class="form-label">Rating (1 to 5)</label>
                    <input type="range" class="form-range" min="1" max="5" id="rating" name="review[rating]" required>
                </div>
                <div class="comment mt-3 mb-3">
                    <label for="comment" class="form-label">Comments</label>
                    <textarea name="review[comment]" class="form-control" id="comment" rows="5" cols="30"
                        required></textarea>
                    <div class="invalid-feedback">please enter comment</div>
                </div>
                <button class="btn btn-outline-dark">Submit</button>
            </form>
            <hr>
            <p><b>All Reviews</b></p>
            <div class="row">
                <% for( review of post.reviews) { %>
                    <div class="card col-5 mb-3 ms-3 ">
                        <h5 class="card-title">User name </h5>
                        <h6 class="card-subtitle mb-2 text-body-secondary">
                            <%= review.comment %>
                        </h6>
                        <p class="card-text">
                            <%= review.rating%> stars
                        </p>
                        <form action="/listing/<%=post._id %>/reviews/<%= review._id%>?_method=DELETE" method="post">
                            <button class="btn btn-dark mb-3"> Delete</button>
                        </form>
                    </div>
                    <% } %>
            </div>
        </div>
    </div>